name: EPG Update
on:
  push:
  schedule:
    - cron: '30 4,10,16,22 * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Update EPG Files
      run: |
        rm -f e.xml e.xml.gz && wget https://raw.githubusercontent.com/sparkssssssssss/epg/main/pp.xml -O e.xml \
        https://raw.githubusercontent.com/sparkssssssssss/epg/main/pp.xml.gz -O e.xml.gz
        git config --local user.name "baihy99"
        git config --local user.email "baihy99@users.noreply.github.com"
        git add e.xml e.xml.gz
        git commit -m "EPG Automatic Update."
    - name: Push changes
      run: git push origin main
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
